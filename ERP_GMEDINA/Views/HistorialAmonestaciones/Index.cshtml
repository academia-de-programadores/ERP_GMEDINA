@model IEnumerable<ERP_GMEDINA.Models.tbHistorialAmonestaciones>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutBase.cshtml";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-sm-4">
        <h2>tbHistorialAmonestaciones</h2>
        <ol class="breadcrumb">
            <li class="active">
            </li>
        </ol>
    </div>
</div>

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="float-e-margins">
                <div class="ibox-title">
                    <h5>Listado de tbHistorialAmonestaciones</h5>
                    <div class="ibox-tools">
                       @Html.ActionLink("Registrar tbHistorialAmonestaciones", "Create", null, new { @class = "btn btn-primary btn-xs"})
                    </div>
                </div>
                <div class="ibox-content">
                  <table class="table table-striped table-bordered table-hover dataTables-example" id="IndexTable">
                    <thead>
                        <tr role="row">
                            <th>@Html.DisplayNameFor(model => model.emp_Id)</th>
                            <th>@Html.DisplayNameFor(model => model.hamo_Fecha)</th>
                            <th>@Html.DisplayNameFor(model => model.tbTipoAmonestaciones.tamo_Descripcion)</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                      @foreach (var item in Model) {
                        <tr class="gradeA odd" role="row" data-id="@item.hamo_Id">
                            <td>
                                @Html.DisplayFor(modelItem => item.emp_Id)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.hamo_Fecha)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.tbTipoAmonestaciones.tamo_Descripcion)
                            </td>
                            <td>
                              @Html.ActionLink("Detalles", "Details", new { id=item.hamo_Id },  new {@class = "btn btn-primary btn-xs" })
                              @Html.ActionLink("Editar", "Edit", new { id=item.hamo_Id }, new {@class = "btn btn-default btn-xs" })
                            </td>
                        </tr>
                      }
                    </tbody>
                  </table>
                </div>
            </div>
        </div>
    </div>
 </div>
    
@section Styles {
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
}

@section Scripts {
  @Scripts.Render("~/plugins/dataTables")
}
