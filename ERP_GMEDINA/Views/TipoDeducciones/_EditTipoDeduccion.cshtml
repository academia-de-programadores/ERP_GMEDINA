@model ERP_GMEDINA.Models.tbTipoDeduccion
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "frmTipoDeduccionEdit" }))
{
    @Html.AntiForgeryToken()
    <div class="modal-body" id="Editar">
        @Html.HiddenFor(model => model.tde_IdTipoDedu)
        @Html.HiddenFor(model => model.tde_UsuarioCrea)
        @Html.HiddenFor(model => model.tde_FechaCrea)

        <!-- CAJAS DE TEXTO -->
        <div class="ibox-content" style="border-top-color:#fff;">
            <div class="form-horizontal">
                <div class="form-group">
                    <div class="row">
                        <div class="col-md-6">
                            @Html.LabelFor(model => model.tde_Descripcion, htmlAttributes: new { /*@class = "control-label col-md-3 pull-left" */})<label class="asterisco" id="AsteriskDescripcionE">*</label>
                        </div>
                        <div class="col-md-6">
                            @Html.EditorFor(model => model.tde_Descripcion, new { htmlAttributes = new { @class = "form-control" } })
                            <label class="text-danger" id="Validation_DescripcionE" style="display:none; opacity: 1; font-family: open sans, Helvetica Neue, Helvetica, Arial, sans-serif; font-weight: normal;">Campo Descripción requerido.</label>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-white" id="btnCerrarEditar">Cerrar</button>
        <button type="submit" class="btn btn-primary pull-left" id="btnUpdateTipoDeducciones">Actualizar</button>
        <button type="button" class="btn btn-danger pull-left" id="btnInactivarTipoDeducciones">Inhabilitar</button>
    </div>
}